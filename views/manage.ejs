<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <script src="https://kit.fontawesome.com/1f3965e820.js" crossorigin="anonymous"></script>
    <title>this's Shop</title>
</head>
<body>
    <%if(login) {%>
        <%- include('navbarLogin', {username: username}) %>
      <%}else {%>
        <%- include('navbarNoLogin') %>
      <%}%>
    <div class="container mt-4 text-center">
        <div class="d-flex justify-content-center">
            <h3><i class="fa-solid fa-gear"></i> Manage</h3>
            <a class="btn btn-success" style="position:absolute; right: 16.2%;" href="/addproduct"><i class="fa-solid fa-file-medical"></i> New product</a>
        </div>
       <table class="table table-striped">
           <thead>
               <tr>
                    <th scope="col">Title</th>
                    <th scope="col">Price</th>
                    <th scope="col">Picture</th>
                    <th scope="col">Product id</th>
                    <th scope="col">Management</th>
               </tr>
           </thead>
           <tbody>
               <% Object.values(products).forEach(item => {%>
               <tr>
                   <td><%= item.name %></td>
                   <td><%= item.price %></td>
                   <td><%= item.picture %></td>
                   <td><%= item._id %></td>
                   <td style="width: 20%;">
                        <a href="/modify/<%= item._id %>" class="btn btn-primary">
                            <i class="fa-solid fa-screwdriver"></i> modify</a>
                        <a href="/delete/<%= item._id %>" onclick="return confirm('Are you sure you want to delete <%= item.name %> ?')" class="btn btn-danger">
                            <i class="fa-solid fa-trash-can"></i> delete</a>
                   </td>
               </tr>
               <%})%>
           </tbody>
       </table>
    </div>
</body>
</html>