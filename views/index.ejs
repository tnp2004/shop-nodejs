<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <script src="https://kit.fontawesome.com/1f3965e820.js" crossorigin="anonymous"></script>
    <title>Clothes Shop</title>
</head>
<body>
    <%if(login) {%>
      <%- include('navbarLogin', {username: username}) %>
    <%}else {%>
      <%- include('navbarNoLogin') %>
    <%}%>
    <div class="container mt-4 my-5">
        <div class="row row-cols-1 row-cols-md-3 g-4">
          <% Object.values(products).forEach(item => {%>
            <div class="col">
              <div class="card" style="height: 100%;">
                <input type="hidden" value="<%= item._id %>" name="id">
                <a href="/productInfo/<%= item._id %>" class="mx-auto">
                    <img src="pictures/products/<%= item.picture %>" class="card-img-top p-5" alt="<%= item.name %>" style="width: 300px;">
                </a>
                <div class="card-body">
                  <h5 class="card-title"><a class="text-decoration-none text-dark" href="/productInfo/<%= item._id %>"><%= item.name %></a>  <i class="fa-solid fa-star"></i> 3.5</h5>
                  <h6 class="card-text">$ <%= item.price %></h6>
                  <p class="card-text text-truncate"><%= item.description %></p>
                </div>
              </div>
            </div>
            <% }) %>
          </div>
    </div>
</body>
</html>